/*Crea un array di numeri interi e fai la somma di tutti gli elementi che sono in posizione dispari.*/

// Creo un array con dalcuni numeri
const array=[2, 1, 3, 1, 4, 2, 5];

// Creo variabile somma inizializzandola a 0
let somma=0;